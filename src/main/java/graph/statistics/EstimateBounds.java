package graph.statistics;

/**
 * Used to set parameters for statistics (see DataSketch documentation on estimation bounds)
 */

public enum EstimateBounds {
    ESTIMATE,
    LOWERBOUND,
    UPPERBOUND
}
